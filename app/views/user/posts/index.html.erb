<div class="container">
 <div class="row">
  <h3>みんなの投稿</h3>
   <table class="table table-hover table-inverse table-sm">
    <tr>
     <th>タイトル</th>
     <th>ユーザー名</th>
    </tr>
    <% @posts.each do |post| %>
    <tr>
     <td><%= post.title %></td>
     <td><%= post.user.owner_name %></td>
    </tr>
   <% end %>
   </table>
  </div>
 </div>

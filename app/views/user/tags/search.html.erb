<h2>検索結果 "<%= @tag_name %>"</h2>
  <ul>
    <% @tag_lists.each do |list| %>
      <li>
        <%#= link_to post_tags_path(post_tag_id: list.id) do %>
          <%= list.tag_name %>
        <%# end %>
      </li>
    <% end %>
  </ul>

  <h4>タグが<%= @tag.tag_name %>の投稿一覧</h4>
  <% @posts.each do |post| %>
  <%= link_to post_path(post.id) do %>
  <%= post.title %>
  <% end %>
  <% end %>